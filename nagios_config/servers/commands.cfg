
# Generic check_ping
define command {
    command_name    check_ping
    command_line    $USER1$/check_ping -H $HOSTADDRESS$ -w 100.0,20% -c 500.0,60%
}

# Check CPU usage on Windows/Linux (via NRPE or check_wmi_plus)
define command {
    command_name    check_cpu
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_cpu
}

# Check Memory usage on Windows/Linux
define command {
    command_name    check_memory
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_memory
}

# Check Disk usage
define command {
    command_name    check_disk
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_disk
}

# Check service status
define command {
    command_name    check_service
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_service -a $ARG1$
}

# Check Windows Eventlog
define command {
    command_name    check_eventlog
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_eventlog
}

# Check logged-in users
define command {
    command_name    check_users
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_users
}

# Check file age
define command {
    command_name    check_file_age
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_file_age -a $ARG1$
}

# Check open files
define command {
    command_name    check_open_files
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_open_files
}

# Check HTTP
define command {
    command_name    check_http
    command_line    $USER1$/check_http -H $HOSTADDRESS$ -p $ARG1$
}

# Check APT updates
define command {
    command_name    check_apt
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_apt
}

# Check specific process
define command {
    command_name    check_procs
    command_line    $USER1$/check_procs -c 1: -C $ARG1$
}

# Check system load
define command {
    command_name    check_load
    command_line    $USER1$/check_load -w 5.0,4.0,3.0 -c 10.0,6.0,4.0
}

# Check log errors (Apache logs for example)
define command {
    command_name    check_log
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_log
}

# Check Nginx status
define command {
    command_name    check_nginx_status
    command_line    $USER1$/check_nginx_status.sh $HOSTADDRESS$
}

# Check SSL certificate expiry
define command {
    command_name    check_ssl_cert
    command_line    $USER1$/check_http -H $HOSTADDRESS$ -S -C 10,5
}

# Check WSUS pending updates (via PowerShell NRPE)
define command {
    command_name    check_updates
    command_line    $USER1$/check_nrpe -H $HOSTADDRESS$ -c check_updates
}

# Check MySQL connection
define command {
    command_name    check_mysql
    command_line    $USER1$/check_mysql -H $HOSTADDRESS$ -u $ARG1$ -p $ARG2$
}

# Check MySQL query
define command {
    command_name    check_mysql_query
    command_line    $USER1$/check_mysql_query -H $HOSTADDRESS$ -q "$ARG1$" -u $ARG2$ -p $ARG3$
}
